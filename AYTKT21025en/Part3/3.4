3.4

FRONTEND AND BACKEND ORIGINAL SIZE
==================================


vyyalacn@UbuntuDocker:~/devops$ docker images|grep end
33_backend                              latest              005c43e12fd5        46 hours ago        383MB
33_frontend                             latest              96071d068342        47 hours ago        463MB


FRONTEND AND BACKEND NEW SIZE
==================================

34_backend                              latest              4996b28fb0f6        45 seconds ago      97.1MB
34_frontend                             latest              6d8af97a1a98        2 minutes ago       257MB




vyyalacn@UbuntuDocker:~/devops/3.4$ cat frontend/Dockerfile
FROM node:alpine

ENV API_URL=http://localhost:8000

RUN apk add -y git && \
    git clone https://github.com/docker-hy/frontend-example-docker.git /app && \
    cd /app && \
    #curl -sL https://deb.nodesource.com/setup_10.x | bash && \
    npm install webpack && \
    addgroup -S app && \
    adduser -S app && \
    chown -R app:app /app


EXPOSE 5000

USER app

WORKDIR /app

#CMD ["node"]


vyyalacn@UbuntuDocker:~/devops/3.4$ cat backend/Dockerfile
FROM node:alpine

ENV FRONT_URL=http://localhost:5000

RUN apk add git && \
    git clone https://github.com/docker-hy/backend-example-docker.git /app && \
    addgroup -S app && \
    adduser -S app && \
    chown -R app:app /app

EXPOSE 8000

USER app

WORKDIR /app

#CMD ["node"]
